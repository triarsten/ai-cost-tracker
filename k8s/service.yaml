apiVersion: v1
kind: Service
metadata:
  name: ai-cost-tracker
  namespace: staging
  labels:
    app: ai-cost-tracker
spec:
  selector:
    app: ai-cost-tracker
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800
      protocol: TCP
  type: NodePort
